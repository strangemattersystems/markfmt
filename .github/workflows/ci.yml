name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  ci:
    name: ci (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            task: ci
          - os: macos-latest
            task: test
          - os: windows-latest
            task: test
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true
          cache-dependency-path: tools/go.sum
      - uses: arduino/setup-task@c0bc642852239c2689f73f4ea6459c29405f3c52 # v3.0.0
        with:
          version: 3.x
          repo-token: ${{ secrets.GITHUB_TOKEN }}
      - run: task ${{ matrix.task }}
      - if: matrix.os == 'ubuntu-latest'
        run: task long
      # FuzzSource is what users run, which keeps the bytes of a block that the
      # printer cannot write. FuzzFormat is left to the nightly workflow: it
      # fails on such a block, and some stay open.
      - if: matrix.os == 'ubuntu-latest'
        name: Fuzz the parser, the comparison and the formatter for a minute each
        run: |
          task fuzz FUZZ=FuzzParse -- -fuzztime=60s
          task fuzz FUZZ=FuzzEqual -- -fuzztime=60s
          task fuzz FUZZ=FuzzSource -- -fuzztime=60s
