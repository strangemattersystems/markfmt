# cmark-gfm regression tests that TestParse expects to fail, one example ID per
# line. The list only gets shorter: remove an ID when its example passes.
#
# The first list was generated once, from an empty list, with:
#
#   go test ./internal/markdown -run '^TestParse$/^cmark-gfm-regression$' 2>&1 |
#     sed -n 's/^ *--- FAIL: TestParse\/cmark-gfm-regression\/cmark-gfm-regression_example_\([0-9]*\) .*/\1/p' |
#     sort -n
13
20
21
22
23 # footnotes: passed only while emphasis did not exist; footnote references are stage 4
24 # footnotes: passed only while strikethrough did not exist; footnote references are stage 4
